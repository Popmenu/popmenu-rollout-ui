<div class="flex items-center justify-end mb-6">
  <div class="inline-flex rounded-lg bg-white p-1 shadow-sm">
    <button 
      type="button" 
      id="team-view-btn"
      onclick="setView('team')"
      class="view-toggle-btn px-4 py-2 text-sm font-medium rounded-md transition-all duration-200"
    >
      <span class="flex items-center gap-2"><%= icon_team %>Team View</span>
    </button>
    <button 
      type="button" 
      id="list-view-btn"
      onclick="setView('list')"
      class="view-toggle-btn px-4 py-2 text-sm font-medium rounded-md transition-all duration-200"
    >
      <span class="flex items-center gap-2"><%= icon_list %>List View</span>
    </button>
  </div>
</div>

<script>
  function setView(view) {
    const listView = document.getElementById('list-view');
    const teamView = document.getElementById('team-view');
    const listBtn = document.getElementById('list-view-btn');
    const teamBtn = document.getElementById('team-view-btn');
    
    // Reset all buttons
    listBtn.classList.remove('active');
    teamBtn.classList.remove('active');
    listBtn.style.backgroundColor = '';
    listBtn.style.color = '';
    teamBtn.style.backgroundColor = '';
    teamBtn.style.color = '';
    
    if (view === 'list') {
      listView.classList.remove('hidden');
      teamView.classList.add('hidden');
      listBtn.classList.add('active');
    } else {
      listView.classList.add('hidden');
      teamView.classList.remove('hidden');
      teamBtn.classList.add('active');
    }
    
    // Save preference to localStorage
    localStorage.setItem('rollout-view-preference', view);
  }
  
  // Initialize view from localStorage on page load
  document.addEventListener('DOMContentLoaded', function() {
    const savedView = localStorage.getItem('rollout-view-preference') || 'team';
    setView(savedView);
  });
</script>
